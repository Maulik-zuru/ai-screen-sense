# If you change POSTGRES_PASSWORD, update the password in DATABASE_URL to match —
# docker-compose.yml reads POSTGRES_PASSWORD from this same file.
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ai_screen_sense
KEY_ENCRYPTION_SECRET=change-me-32-byte-base64-secret
PORT=8787
