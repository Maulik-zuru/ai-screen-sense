# If you change POSTGRES_PASSWORD or POSTGRES_PORT, update DATABASE_URL to match —
# docker-compose.yml reads both from this same file.
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5433
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/ai_screen_sense
KEY_ENCRYPTION_SECRET=change-me-32-byte-base64-secret
PORT=8787
