{
  "name": "@ai-screen-sense/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo \"(shared is consumed as TS source directly, no build step needed)\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint configured for shared)\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
